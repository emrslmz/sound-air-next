import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.soundairApp.android',
  appName: 'sound_air_android_app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
